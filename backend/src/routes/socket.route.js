import { Router } from "express";

const router = Router();

// This file is intentionally left minimal.
// Socket.IO is attached in server bootstrap (see modifications in backend/server.js/app wiring).

export default router;

